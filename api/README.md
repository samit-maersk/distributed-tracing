# api-n-kafka-producer
To run this demo:
- Start the application
```shell
./mvnw spring-boot:run
```

- create a native executable
```shell
./mvnw -Pnative native:compile 
```
- To run the native executable
```shell
  ./target/api-n-kafka-producer
```